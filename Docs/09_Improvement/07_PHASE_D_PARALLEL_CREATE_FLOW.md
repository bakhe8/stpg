# 07 - Phase D Parallel Create Flow

## حالة الوثيقة

هذه هي حزمة Phase D داخل `Docs/09_Improvement`.

الحالة الحالية: اكتمل التنفيذ بشرائح آمنة، وأصبح المسار الجديد default مع بقاء rollback عبر feature flag.

## الهدف

بناء مسار إنشاء جديد للمستخدم العادي يفصل بين:

- صندوق دائم.
- حملة مؤقتة مرتبطة بصندوق قائم.

مع بقاء شاشة الإنشاء القديمة متاحة كمسار رجوع إلى أن يثبت parity pack.

## حدود Phase D

Phase D لا تعني حذف القديم.

لا نفعل في هذه المرحلة:

- لا نحذف `/entities/new`.
- لا نحذف `EntityType`.
- لا نغير `/entities` إلى `/funds`.
- لا نجعل المسار الجديد default قبل التحقق.
- لا ننشئ campaign مستقلة بلا parent fund.

## Feature Flag

العلم المستخدم:

- `NEXT_PUBLIC_ENABLE_FUND_CREATE_FLOW`

السلوك:

| القيمة | النتيجة |
|---|---|
| `false` | شاشة الإنشاء القديمة تبقى كمسار rollback مؤقت. |
| `true` أو غير موجود | تظهر شاشة اختيار `صندوق` أو `حملة` داخل `/entities/new`. |

## ما تم في أول شريحة

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-001` | منفذة | إضافة feature flag في frontend. |
| `D-002` | منفذة | شاشة دخول جديدة تفصل صندوق/حملة عند تفعيل flag. |
| `D-003` | منفذة جزئيا | إنشاء صندوق من المسار الجديد يستخدم `type = COMMUNITY` داخليا، ويحفظ `profileKey/profileLabel` اختياريا. |
| `D-004` | منفذة جزئيا | إنشاء حملة يستخدم endpoint الحالي `POST /entities/:id/campaigns` ويتطلب اختيار صندوق قائم. |
| `D-005` | منفذة | زر رجوع إلى النموذج القديم داخل المسار الجديد. |

## ما تم في شريحة D-006

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-006` | منفذة | أضيف setup checklist في صفحة الصندوق للمؤسس/المدير عندما يكون الصندوق غير مكتمل التشغيل. |

الـ checklist الحالية لا تضيف backend contract جديدا. هي طبقة واجهة محسوبة من البيانات الموجودة:

- وصف الصندوق.
- بيانات البنك.
- وجود محفظة نشطة.
- وجود مسار حوكمة نشط داخل المحافظ.
- وجود أعضاء غير المؤسس.

هذا يحقق قرار المنتج: نموذج الإنشاء يبقى خفيفا، والتخصيص ينتقل إلى ما بعد الإنشاء بدون إخفاء القدرات.

## ما تم في شريحة D-007

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-007` | منفذة | أضيفت خريطة تشغيل الحملة داخل مسار الإنشاء الجديد. |

خريطة الحملة تعرض للمؤسس قبل الإنشاء:

- الصندوق الأب الذي سترتبط به الحملة.
- مدة الحملة أو الحاجة إلى إغلاق يدوي لاحقا.
- جاهزية بيانات بنك الصندوق الأب.
- أن الصرف والمراجعة سيحتاجان مسار/محفظة حملة بعد الإنشاء.

لم نوسع `CreateCampaignDto` في هذه الشريحة. السبب أن backend الحالي يدعم:

- `name`
- `description`
- `campaignEndsAt`

لذلك لا ترسل الواجهة حقولا تشغيلية جديدة قد يرفضها الباكند. التفاصيل الجديدة هي طبقة فهم/معاينة، وليست schema جديدا.

## ما تم في شريحة D-008

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-008` | منفذة | تبسيط عرض القوالب في مسار إنشاء الصندوق الجديد كاختيارات تشغيلية بدلا من كشف تفاصيل القالب التقنية. |

المسار الجديد يعرض للمؤسس الآن:

- بدء فارغ.
- صندوق مرن.
- تكافل ومساعدات.
- خدمات مشتركة.
- دعم وتبرعات.

هذه الاختيارات ما زالت تستخدم `EntityTemplate` الحالي و`templateId` نفسه عند الإنشاء. التغيير هو طبقة عرض فقط:

- لا تغيير في seed القوالب.
- لا تغيير في backend contract.
- لا حذف لتفاصيل القوالب من المسار القديم.
- لا تغيير في تطبيق المحافظ أو المسارات الذي ثبته Phase B.

## ما تم في شريحة D-009

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-009` | منفذة | أضيف parity pack backend يغطي القدرات الأساسية قبل أي default switch. |

مرجع التفاصيل:

- `Docs/09_Improvement/08_PHASE_D_PARITY_PACK.md`

التحقق المضاف:

- `backend/src/entities/phase-d-parity.spec.ts`
- `backend/src/entities/entities.service.spec.ts`
- `backend/package.json` script: `test:phase-d:parity`

هذه الشريحة لا تختبر الواجهة بصريا، ولا تجعل المسار الجديد default. اختبار UX للعلمين يبقى في `D-010`.

## ما تم في شريحة D-010

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-010` | منفذة | أضيف UX smoke test للعلمين: flag off وflag on. |

مرجع التفاصيل:

- `Docs/09_Improvement/09_PHASE_D_UX_SMOKE_TESTS.md`

التحقق المضاف:

- `frontend/scripts/phase-d-create-flow-smoke.spec.cjs`
- `frontend/package.json` script: `test:phase-d:create-flow`

هذه الشريحة لا تنشئ صندوقا أو حملة فعليا. هي تثبت سلوك الواجهة:

- عند تعطيل العلم، تظهر شاشة الإنشاء القديمة بتصنيفات عائلة/عمارة/حي/قبيلة/حملة مؤقتة.
- عند تفعيل العلم، تظهر شاشة صندوق/حملة الجديدة.
- زر الرجوع إلى النموذج القديم يبقى متاحا داخل المسار الجديد.
- شاشة الصندوق تعرض نقطة بدء فارغة والقوالب التشغيلية المبسطة.
- شاشة الحملة تعرض خريطة تشغيل الحملة والصندوق الأب وما بعد الإنشاء.

## ما تم في شريحة D-011

| التذكرة | الحالة | التفاصيل |
|---|---|---|
| `D-011` | منفذة | أصبح مسار صندوق/حملة هو default، وبقيت قيمة `NEXT_PUBLIC_ENABLE_FUND_CREATE_FLOW=false` rollback للنموذج القديم. |

مرجع التفاصيل:

- `Docs/09_Improvement/10_PHASE_D_DEFAULT_SWITCH.md`

التحقق المضاف:

- توسع `frontend/scripts/phase-d-create-flow-smoke.spec.cjs` ليغطي default/empty value بالإضافة إلى `true` و`false`.

## ما لم يكتمل بعد

لا يوجد بند مفتوح داخل Phase D بعد `D-011`.

## قرارات المنتج المثبتة

- الصندوق الجديد يبدأ كـ `COMMUNITY` داخليا إذا لم يكن هناك سبب تشغيلي لاستخدام نوع قديم.
- عائلة/عمارة/حي/قبيلة أصبحت profile اختياري، لا تقود السلوك.
- الحملة لا تنشأ منفصلة في المرحلة الأولى؛ يجب ربطها بصندوق قائم.
- النموذج القديم يبقى متاحا للفريق والمقارنة.

## ضمان عدم خسارة القدرات

المسار الجديد لا يختصر الباكند:

- اختيار القالب ما زال يطبق Phase B ويُنشئ المحافظ والمسارات.
- الصندوق الفارغ يبقى قابلا للتوسيع لاحقا بالمحافظ والمسارات والقواعد.
- عرض القوالب المبسط لا يغير `templateId` ولا يمنع إنشاء نفس المحافظ والمسارات الافتراضية.
- setup checklist يجعل نقص المحفظة/المسار/البنك/الأعضاء ظاهرا بعد الإنشاء بدلا من فرضه في wizard.
- الإعدادات المتقدمة تبقى في مسارها الحالي وتخضع لصلاحية Phase C.
- الحملة تستخدم lifecycle الحالي: parent fund، `isCampaign`, و`campaignEndsAt`.
- شاشة إنشاء الحملة لا توهم المستخدم بأن هدفا ماليا أو محفظة حملة أُنشئت تلقائيا.

## شروط إغلاق Phase D

لا تعتبر Phase D مغلقة إلا إذا تحقق التالي:

1. flag off يحافظ على legacy flow بدون تغير.
2. flag on يسمح بإنشاء صندوق من المسار الجديد.
3. flag on يسمح بإنشاء حملة تابعة لصندوق قائم.
4. صندوق بلا قالب يظهر له setup checklist واضح.
5. صندوق بقالب ينشئ نفس البنية التشغيلية المثبتة في Phase B.
6. لا تظهر الحملة كنوع صندوق عادي في المسار الجديد.
7. parity pack يغطي التصويت، اللجان، المال، التدقيق، الصرف، الاشتراكات، النزاعات، الحملات، والمحافظ المتعددة. تم في `D-009`.
8. UX smoke test يثبت سلوك العلمين ومسار الرجوع. تم في `D-010`.
9. default switch تم مع rollback صريح. تم في `D-011`.

## التحقق المطلوب لهذه الشريحة

- JSON parse لترجمات `ar/en admin`.
- `npm run build` في `frontend`.
- `npm run lint` في `frontend`.

Backend لا يتغير في D-006 لأنها طبقة checklist واجهية محسوبة من endpoints قائمة.

Backend لا يتغير في D-007 لأن الحملة ما زالت تستخدم عقد `CreateCampaignDto` الحالي فقط.

Backend لا يتغير في D-008 لأن الاختيارات التشغيلية تستخدم القوالب الحالية و`templateId` نفسه.

D-009 يضيف tests فقط ولا يغير runtime أو schema.

D-010 يضيف Playwright smoke test فقط ولا يغير runtime أو schema.

D-011 يغير default الواجهة فقط ولا يغير backend أو schema.
